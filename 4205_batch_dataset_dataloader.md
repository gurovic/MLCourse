## 🟢 Уровень 1: Базовый — Фундаментальные навыки

### Задача 1.1: Создание полного пайплайна для линейной регрессии

**Контекст:** Вы строите модель для предсказания цен на квартиры по 5 признакам (площадь, этаж, расстояние до метро, год постройки, количество комнат).

**Данные:**
```python
import torch
import numpy as np

# Генерируем синтетические данные
np.random.seed(42)
n_samples = 1000
features = np.random.randn(n_samples, 5) * 10 + 50  # 5 признаков
# Цена = 1000*площадь + 500*этаж - 200*расстояние + ... + шум
prices = (
    features[:, 0] * 1000 +     # площадь
    features[:, 1] * 500 +      # этаж
    features[:, 2] * (-200) +   # расстояние до метро
    features[:, 3] * 50 +       # год постройки
    features[:, 4] * 300 +      # комнаты
    np.random.randn(n_samples) * 5000  # шум
)
```

**Задание:**
1. Создайте класс `HousingDataset`, который наследуется от `torch.utils.data.Dataset`
2. Разделите данные на тренировочные (70%), валидационные (15%) и тестовые (15%) используя `random_split`
3. Создайте три DataLoader с batch_size=32 (для train shuffle=True, для val/test shuffle=False)
4. Напишите функцию, которая проходит по одному батчу из train_loader и вычисляет:
   - Среднее значение признаков в батче
   - Стандартное отклонение признаков в батче
   - Корреляцию между площадью и ценой в батче

---

### Задача 1.2: Dataset для классификации фруктов по изображениям

**Контекст:** Вы создаете мобильное приложение для определения фруктов по фото. У вас есть папка с изображениями, организованная по подпапкам с названиями фруктов.

**Структура данных:**
```
fruits_dataset/
├── apple/
│   ├── apple_001.jpg
│   ├── apple_002.jpg
│   └── ...
├── banana/
│   ├── banana_001.jpg
│   └── ...
└── orange/
    ├── orange_001.jpg
    └── ...
```

**Задание:**
1. Создайте класс `FruitDataset`, который:
   - Сканирует папку и создает список путей к изображениям и соответствующих меток
   - Преобразует метки (имена папок) в числовые индексы: apple→0, banana→1, orange→2
   - Загружает изображения с помощью PIL и применяет базовые трансформации

2. Реализуйте трансформации для изображений:
   ```python
   from torchvision import transforms
   
   train_transform = transforms.Compose([
       transforms.Resize((224, 224)),
       transforms.RandomHorizontalFlip(),
       transforms.ToTensor(),
       transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225])
   ])
   
   val_transform = transforms.Compose([
       transforms.Resize((224, 224)),
       transforms.ToTensor(),
       transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225])
   ])
   ```

3. Создайте DataLoader для тренировки и валидации с правильными параметрами

---

## 🟡 Уровень 2: Средний — Интеграция в ML пайплайн

### Задача 2.1: Обучение CNN с аугментацией данных

**Контекст:** Вы создаете модель для классификации рукописных цифр (MNIST-like) с ограниченным количеством данных.

**Модель:**
```python
import torch.nn as nn
import torch.nn.functional as F

class DigitCNN(nn.Module):
    def __init__(self):
        super().__init__()
        self.conv1 = nn.Conv2d(1, 32, 3, padding=1)
        self.conv2 = nn.Conv2d(32, 64, 3, padding=1)
        self.pool = nn.MaxPool2d(2, 2)
        self.fc1 = nn.Linear(64 * 7 * 7, 128)
        self.fc2 = nn.Linear(128, 10)
        self.dropout = nn.Dropout(0.25)
    
    def forward(self, x):
        x = self.pool(F.relu(self.conv1(x)))
        x = self.pool(F.relu(self.conv2(x)))
        x = x.view(-1, 64 * 7 * 7)
        x = F.relu(self.fc1(x))
        x = self.dropout(x)
        x = self.fc2(x)
        return x
```

**Задание:**
1. Создайте **расширенный Dataset** с сильной аугментацией:
   ```python
   train_transform = transforms.Compose([
       transforms.RandomRotation(15),
       transforms.RandomAffine(degrees=0, translate=(0.1, 0.1)),
       transforms.RandomResizedCrop(28, scale=(0.8, 1.0)),
       transforms.ColorJitter(brightness=0.2, contrast=0.2),
       transforms.ToTensor(),
       transforms.Normalize((0.5,), (0.5,))
   ])
   ```

2. Реализуйте **динамическую аугментацию**:
   - В начале обучения: слабая аугментация
   - По мере обучения: увеличивайте силу аугментации

3. Напишите тренировочный цикл, который:
   - Использует GPU если доступен
   - Сохраняет лучшую модель по accuracy на валидации
   - Визуализирует аугментированные изображения из батча

**Требование к качеству:** > 99% accuracy на тестовом наборе за 10 эпох.

---

### Задача 2.2: Балансировка классов для задачи обнаружения аномалий

**Реальный сценарий:** Вы анализируете логи веб-сервиса для обнаружения кибератак. Атаки редки (< 0.1% запросов), но критически важны.

**Данные:**
```python
# Симуляция логов веб-сервиса
n_requests = 1_000_000
features = np.random.randn(n_requests, 10)  # 10 признаков

# Генерируем атаки (класс 1)
attack_indices = np.random.choice(n_requests, size=1000, replace=False)  # 0.1% атак
labels = np.zeros(n_requests)
labels[attack_indices] = 1

# Атаки имеют другие статистики
features[attack_indices] += np.random.randn(1000, 10) * 3
```

**Задание:**
1. Создайте `ImbalancedDataset` с использованием `WeightedRandomSampler`:
   - Вычислите веса классов автоматически
   - Реализуйте параметр `sampling_strategy`:
     - `'balanced'`: равное количество каждого класса
     - `'minority_boost'`: увеличение веса миноритарного класса в N раз
     - `'auto'`: веса обратно пропорциональны частоте классов

2. Реализуйте **стратифицированное разделение** с сохранением пропорций классов:
   - Даже при 0.1% атак, в каждой части (train/val/test) должно быть ~0.1% атак

3. Добавьте метрики для несбалансированных данных:
   - Precision, Recall, F1-score
   - ROC-AUC

**Важный нюанс:** При оценке модели на тестовых данных **не используйте WeightedRandomSampler** — тест должен отражать реальное распределение данных.

---

### Задача 2.3: Multi-task Dataset для нескольких целей

**Контекст:** Вы разрабатываете систему для анализа автомобилей по фото:
- Классификация типа автомобиля (седан, SUV, грузовик)
- Регрессия для предсказания года выпуска
- Бинарная классификация: поврежден/не поврежден

**Задание:**
1. Создайте `MultiTaskCarDataset`, который возвращает словарь:
   ```python
   {
       'image': torch.Tensor(3, 256, 256),  # RGB изображение
       'car_type': torch.LongTensor(1),     # 0=седан, 1=SUV, 2=грузовик
       'year': torch.FloatTensor(1),        # 1990-2023
       'damaged': torch.LongTensor(1)       # 0=нет, 1=да
   }
   ```

2. Реализуйте разные трансформации для разных задач:
   - Для классификации: сильная аугментация
   - Для регрессии: только resize и normalize (сохраняем информацию о годе)

3. Создайте кастомную `collate_fn` для обработки:
   - Изображений разного размера (патчинг до максимального в батче)
   - Отсутствующих меток (не все изображения имеют все три метки)

---

## 🔴 Уровень 3: Продвинутый — Production-решения

### Задача 3.1: Оптимизация загрузки данных для обучения на GPU

**Контекст:** Вы обучаете модель на большом датасете изображений (50,000+ изображений). При использовании стандартного DataLoader GPU простаивает 30% времени, ожидая загрузки данных.

**Задание:**
1. Создайте `OptimizedImageDataset`, который:
   - Использует **memory-mapped файлы** для быстрого доступа к данным
   - Реализует **предварительную загрузку (prefetching)** следующего батча во время тренировки
   - Сохраняет часто используемые изображения в **LRU кэше** в памяти

2. Оптимизируйте DataLoader параметрами:
   - `num_workers` в зависимости от количества CPU ядер
   - `pin_memory=True` для быстрого копирования на GPU
   - `prefetch_factor=2` для загрузки батчей заранее

3. Напишите функцию для измерения производительности:
   ```python
   def benchmark_loader(loader, model, num_batches=100):
       """Измеряет время загрузки и GPU utilization"""
       # Реализуйте измерение времени загрузки и обработки на GPU
       pass
   ```

---

### Задача 3.2: Dataset с динамической аугментацией по сложности

**Контекст:** При обучении модели сложность аугментации должна возрастать. В начале обучения - простые трансформации, к концу - сложные, чтобы модель училась на более трудных примерах.

**Задание:**
1. Создайте `CurriculumDataset`, который:
   - Начинает с простых аугментаций (только resize и normalize)
   - Постепенно увеличивает сложность (добавляет rotation, flip, color jitter)
   - В конце обучения использует сильные аугментации

2. Реализуйте механизм **автоматической настройки аугментаций**:
   - На основе accuracy на валидации
   - На основе loss на тренировочных данных
   - По расписанию (эпохам)

3. Добавьте **адаптивные аугментации**:
   - Для сложных классов - слабые аугментации
   - Для простых классов - сильные аугментации
   - На основе confidence модели

---

### Задача 3.3: Multi-GPU DataLoader с балансировкой нагрузки

**Контекст:** Вы обучаете модель на 4 GPU. Некоторые изображения требуют больше времени для предобработки (разные размеры, сложные трансформации), что приводит к дисбалансу нагрузки между GPU.

**Задание:**
1. Создайте `BalancedMultiGPUDataset`, который:
   - Оценивает сложность обработки каждого примера (по размеру изображения, типу трансформаций)
   - Балансирует нагрузку между GPU, распределяя сложные и простые примеры равномерно
   - Использует `torch.distributed` для синхронизации между процессами

2. Реализуйте **динамическую балансировку**:
   - Мониторинг времени обработки на каждом GPU
   - Перераспределение очереди примеров в реальном времени
   - Компенсация network latency

3. Добавьте **приоритетную обработку**:
   - Примеры с высокой loss получают приоритет в следующей эпохе
   - Сложные классы обрабатываются чаще

---

## 💡 Практические советы для выполнения задач

### Для уровня 🟢:
- Начинайте с создания простого рабочего Dataset, затем добавляйте сложности
- Всегда проверяйте размерности тензоров после загрузки
- Используйте `num_workers=0` для отладки, затем увеличивайте

### Для уровня 🟡:
- Профилируйте загрузку данных с помощью `torch.utils.bottleneck`
- Тестируйте на подмножестве данных перед запуском на полном датасете
- Используйте `torch.cuda.empty_cache()` для управления памятью GPU

### Для уровня 🔴:
- Измеряйте GPU utilization во время загрузки данных
- Реализуйте мониторинг производительности пайплайна
- Оптимизируйте bottleneck'ы на основе реальных измерений

**Золотое правило:** Правильно настроенный DataLoader может ускорить обучение в 2-5 раз. Инвестируйте время в оптимизацию загрузки данных!
