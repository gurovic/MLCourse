# Задачи: Обзор и сравнение библиотек Deep Learning

## Задача 1: Сравнительный анализ синтаксиса фреймворков

**Цель:** Понять различия в подходах и философии разных фреймворков через практику.

**Задание:**
Реализуйте **сверточную нейронную сеть (CNN)** для классификации изображений Fashion-MNIST в **двух** фреймворках на выбор:
- PyTorch
- TensorFlow/Keras
- JAX + Flax (для продвинутых)

**Архитектура CNN:**
- Conv2D(32 фильтра, 3x3) → ReLU → MaxPool2D(2x2)
- Conv2D(64 фильтра, 3x3) → ReLU → MaxPool2D(2x2)
- Flatten → Dense(128) → ReLU → Dropout(0.3) → Dense(10)

**Что нужно сделать:**
1. Загрузите датасет Fashion-MNIST
2. Реализуйте модель в двух выбранных фреймворках
3. Обучите каждую модель на 5 эпох
4. Замерьте время обучения одной эпохи
5. Сравните точность на тестовой выборке

**Вопросы для анализа:**
- Сколько строк кода потребовалось в каждом фреймворке?
- В каком фреймворке было проще отлаживать код?
- Какие концептуальные различия вы заметили (например, статический vs динамический граф)?
- Какой фреймворк вы бы выбрали для своего проекта и почему?

**Подсказка:** Fashion-MNIST можно загрузить через `torchvision.datasets` (PyTorch) или `keras.datasets.fashion_mnist` (TensorFlow).

---

## Задача 2: Исследование ONNX для кроссплатформенности

**Цель:** Научиться переносить модели между фреймворками через ONNX.

**Сценарий:** Вы обучили модель в PyTorch, но в production нужно использовать ONNX Runtime для максимальной скорости.

**Задание:**
1. Обучите **ResNet-18** на CIFAR-10 в PyTorch (3-5 эпох достаточно)
2. Экспортируйте обученную модель в формат ONNX
3. Загрузите модель через ONNX Runtime
4. Напишите функцию `predict_onnx(image)` для инференса
5. Проведите эксперименты:
   - Сравните размеры файлов: `.pth` vs `.onnx`
   - Замерьте время инференса на 100 изображениях (PyTorch vs ONNX Runtime)
   - Проверьте, что предсказания идентичны (допустимо небольшое расхождение)

**Бонус:** 
- Примените динамическую квантизацию (FP32 → INT8) в ONNX
- Измерьте, как изменился размер модели и скорость инференса
- Оцените падение точности (если есть)

**Подсказка:** Для замера времени используйте `time.perf_counter()` и усредните результаты по нескольким запускам.

---

## Задача 3: Миграция архитектуры между фреймворками

**Цель:** Научиться "переводить" модели с одного API на другой.

**Задание:**
Дана спецификация модели для классификации последовательностей:

**Архитектура RNN Classifier:**
- Embedding слой (vocab_size=5000, embed_dim=100)
- Bidirectional LSTM (hidden_size=64, 2 слоя)
- Dropout (0.5)
- Dense слой (128 нейронов) → ReLU
- Выходной Dense слой (num_classes=3)

Реализуйте эту архитектуру в:
1. **PyTorch** (используя `nn.Module`)
2. **TensorFlow/Keras** (используя Functional API)

**Требования:**
- Убедитесь, что количество параметров примерно совпадает (допустимо расхождение ±5%)
- Используйте `.summary()` в Keras и создайте аналогичную функцию для PyTorch
- Добавьте комментарии, объясняющие ключевые различия в реализации

**Вопросы для размышления:**
- Чем отличается обработка bidirectional LSTM в PyTorch и Keras?
- Где легче контролировать размерности тензоров?
- Какой подход вам показался более интуитивным?

---

## Задача 4: Бенчмаркинг производительности

**Цель:** Научиться объективно сравнивать производительность фреймворков.

**Задание:**
Проведите эксперименты с **MobileNetV2** на синтетических данных (чтобы избежать влияния загрузки данных):

**Что измерить:**
1. **Время обучения:**
   - Создайте синтетический датасет: 5000 изображений 224x224x3
   - Обучите MobileNetV2 одну эпоху в PyTorch и TensorFlow/Keras
   - Замерьте время (используйте `time.time()`)

2. **Время инференса:**
   - Замерьте время предсказания для batch_size = 1, 16, 32, 64
   - Используйте PyTorch, TensorFlow, и ONNX Runtime
   - Усредните результаты по 100 запускам

3. **Потребление памяти GPU:**
   - При наличии GPU: используйте `torch.cuda.memory_allocated()` (PyTorch)
   - При отсутствии GPU: пропустите этот пункт

**Оформление:**
- Создайте таблицу с результатами
- Постройте график зависимости времени инференса от batch_size
- Напишите короткие выводы (3-5 предложений)

---

## Задача 5: Fine-tuning с Hugging Face Transformers

**Цель:** Научиться работать с предобученными трансформерами для практических задач.

**Задание:**
Создайте классификатор тональности отзывов на фильмы:

1. **Подготовка:**
   - Загрузите датасет IMDB через `datasets` библиотеку Hugging Face
   - Выберите предобученную модель: `distilbert-base-uncased` (быстрая и легкая)

2. **Fine-tuning:**
   - Используйте `Trainer` API из Hugging Face
   - Обучите на 2000 примерах (для экономии времени)
   - Настройте `TrainingArguments`: 3 эпохи, batch_size=16, lr=2e-5

3. **Оценка и применение:**
   - Оцените accuracy на тестовой выборке
   - Создайте функцию `predict_sentiment(text)` для новых отзывов
   - Протестируйте на 5 собственных примерах текстов

**Творческое задание:**
Попробуйте две модели (`distilbert-base-uncased` и `roberta-base`) и сравните:
- Скорость обучения
- Итоговую точность
- Качество предсказаний на ваших примерах

**Подсказка:** Документация Hugging Face очень подробная, ищите примеры для text classification.

---

## Задача 6: Рефакторинг кода с PyTorch Lightning

**Цель:** Научиться писать чистый, организованный код для обучения нейросетей.

**Задание:**
Возьмите **вашу собственную модель** из предыдущих работ (или любую CNN на MNIST) и выполните рефакторинг:

1. **Создайте LightningModule:**
   ```python
   class MyLitModel(pl.LightningModule):
       def __init__(self, ...):
           # Определите архитектуру
       
       def forward(self, x):
           # Прямой проход
       
       def training_step(self, batch, batch_idx):
           # Шаг обучения: loss computation
       
       def validation_step(self, batch, batch_idx):
           # Шаг валидации
       
       def configure_optimizers(self):
           # Оптимизатор и scheduler
   ```

2. **Настройте Trainer:**
   - Добавьте `EarlyStopping` callback
   - Настройте логирование в TensorBoard
   - Включите `ModelCheckpoint` для сохранения лучшей модели

3. **Анализ:**
   - Посчитайте строки кода: до и после
   - Запустите TensorBoard и изучите графики
   - Напишите, стал ли код понятнее

**Бонус:** Добавьте `LearningRateMonitor` и посмотрите, как изменяется learning rate.

---

## Задача 7: Универсальный предиктор

**Цель:** Создать инструмент для работы с моделями из разных фреймворков.

**Задание:**
Разработайте класс `ModelPredictor`, который умеет работать с моделями разных форматов:

```python
class ModelPredictor:
    def load_model(self, model_path: str):
        """Загружает модель по расширению файла (.pth, .h5, .onnx)"""
        pass
    
    def predict(self, input_data):
        """Делает предсказание независимо от типа модели"""
        pass
    
    def get_model_info(self):
        """Возвращает информацию о модели (framework, размер, etc.)"""
        pass
```

**Требования:**
1. Поддержка форматов: PyTorch (`.pth`), TensorFlow (`.h5`), ONNX (`.onnx`)
2. Автоматическое определение типа модели
3. Единый интерфейс для предсказаний
4. Обработка ошибок (неправильный формат, несовместимые данные)

**Тестирование:**
- Обучите простую модель в каждом фреймворке
- Сохраните в соответствующем формате
- Протестируйте загрузку и предсказание через `ModelPredictor`

**Бонус:** Добавьте поддержку батчевой обработки и preprocessing.

---

## Задача 8: Исследование JAX трансформаций

**Цель:** Понять функциональные трансформации и их мощь в JAX.

**Задание:**
Изучите ключевые трансформации JAX на примере простой функции:

1. **Создайте функцию потерь:**
   ```python
   def mse_loss(params, x, y_true):
       """Вычисляет MSE между предсказанием и истинным значением"""
       # params = {'w': weight, 'b': bias}
       y_pred = params['w'] * x + params['b']
       return jnp.mean((y_pred - y_true)**2)
   ```

2. **Примените трансформации:**
   - `jax.grad()` — получите градиенты по параметрам
   - `jax.jit()` — скомпилируйте функцию для ускорения
   - `jax.vmap()` — векторизуйте для батчевой обработки

3. **Эксперименты:**
   - Замерьте время выполнения без JIT vs с JIT (1000 вызовов)
   - Сравните скорость vmap vs обычного цикла по батчу
   - Вычислите Hessian (вторые производные) используя `jax.hessian()`

**Вопросы:**
- Во сколько раз JIT ускоряет вычисления?
- Почему vmap быстрее, чем обычный цикл?
- Когда стоит использовать JAX вместо PyTorch?

---

## Задача 9: Сравнение инструментов визуализации

**Цель:** Выбрать лучший инструмент для отслеживания экспериментов.

**Задание:**
Обучите **одну и ту же модель** (например, ResNet-18 на CIFAR-10) дважды, используя разные инструменты логирования:

1. **TensorBoard:**
   - Логируйте: train/val loss, train/val accuracy, learning rate
   - Добавьте график архитектуры модели
   - Сохраните несколько примеров предсказаний с изображениями

2. **Weights & Biases (wandb):**
   - То же самое, что с TensorBoard
   - Дополнительно: логируйте гиперпараметры
   - Используйте `wandb.watch()` для отслеживания градиентов

**Анализ (напишите отчет):**
- **Простота интеграции:** сколько строк кода потребовалось?
- **Визуализация:** какой инструмент удобнее для анализа?
- **Дополнительные функции:** что уникального есть у каждого?
- **Совместная работа:** какой инструмент лучше для команды?
- **Ваш выбор:** что бы вы использовали в своем проекте?

---

## Задача 10: Multi-deployment эксперимент

**Цель:** Научиться развертывать модели разными способами и сравнивать подходы.

**Задание:**
Возьмите обученную **модель классификации** (например, ResNet на CIFAR-10) и разверните её двумя способами:

**Вариант А: FastAPI + PyTorch**
```python
from fastapi import FastAPI, File, UploadFile
import torch

app = FastAPI()

@app.post("/predict")
async def predict(file: UploadFile = File(...)):
    # Загрузка изображения
    # Предсказание
    # Возврат результата
    pass
```

**Вариант Б: TensorFlow Serving**
- Экспортируйте модель в SavedModel format
- Запустите TF Serving через Docker
- Отправьте запрос через REST API

**Сравнение:**
1. Время на развертывание (сколько минут?)
2. Latency (время отклика на один запрос)
3. Throughput (запросов в секунду при нагрузке)
4. Простота использования

**Тестирование:**
- Отправьте 10 тестовых изображений
- Замерьте среднее время отклика
- Проверьте корректность предсказаний

---

## Бонусные задачи

### Задача 11: Квантизация для edge-устройств

**Для продвинутых:** Оптимизируйте модель MobileNetV2 для мобильных устройств:

1. Примените динамическую квантизацию в PyTorch
2. Примените статическую квантизацию в TensorFlow Lite
3. Сравните:
   - Размер модели (МБ)
   - Скорость инференса (CPU)
   - Точность (допустимо падение до 2%)

**Цель:** модель <10 МБ с минимальной потерей точности.

### Задача 12: Transfer Learning Battle

Сравните transfer learning в трех фреймворках:
- PyTorch (torchvision.models)
- TensorFlow/Keras (keras.applications)
- Hugging Face (для Vision Transformer)

Fine-tune на небольшом custom датасете (100-200 изображений на класс).
Какой подход дал лучший результат при минимальных данных?

### Задача 13: Apple MLX exploration (для Mac M1/M2/M3/M4)

Если у вас Mac с Apple Silicon:
1. Установите MLX framework от Apple
2. Реализуйте и обучите простую CNN
3. Сравните с PyTorch на том же железе
4. Измерьте: время обучения, память, температуру чипа

---

## Методические рекомендации

**Для учителя:**
- Задачи 1-3: базовый уровень, обязательны для всех
- Задачи 4-7: продвинутый уровень
- Задачи 8-10: для сильных учеников
- Бонусные: по интересам

**Для ученика:**
- Начните с задач 1-2, они заложат фундамент
- Не копируйте код из теории — попробуйте написать сами
- Анализируйте результаты: "почему" важнее "как"
- Документируйте эксперименты (скриншоты, графики, выводы)

**Полезные ресурсы:**
- [PyTorch Tutorials](https://pytorch.org/tutorials/)
- [TensorFlow Guides](https://www.tensorflow.org/guide)
- [Hugging Face Course](https://huggingface.co/course)
- [JAX Quickstart](https://jax.readthedocs.io/en/latest/notebooks/quickstart.html)
